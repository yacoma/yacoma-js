/* eslint-disable @typescript-eslint/no-unused-vars */
import * as validate from './'

/*
On creation by one of the two users, the `users` field is set as
follow and cannot be changed after this (nobody has `u` access):
*/
describe('PairCollection.userAccess', () => {
  it('should x', () => {
    expect(true).toBe(true)
  })
})
